@model IEnumerable<TestCSharpMVC.Models.Usuario>

@{
    Layout = null;
}

<!DOCTYPE html>

<html>
<head>
    <meta name="viewport" content="width=device-width" />
    <title>Index</title>
</head>
<body>
    <p>
        @Html.ActionLink("Create New", "Create")
    </p>
    <table class="table">
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.UsuarioName)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.UsuarioLastName)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.UsuarioPw)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.UsuarioPhase)
            </th>
            <th></th>
        </tr>
    
    @foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.UsuarioName)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.UsuarioLastName)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.UsuarioPw)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.UsuarioPhase)
            </td>
            <td>
                @Html.ActionLink("Edit", "Edit", new { id=item.Id }) |
                @Html.ActionLink("Details", "Details", new { id=item.Id }) |
                @Html.ActionLink("Delete", "Delete", new { id=item.Id })
            </td>
        </tr>
    }
    
    </table>
</body>
</html>
